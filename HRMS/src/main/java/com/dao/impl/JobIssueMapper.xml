<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="com.dao.IJobIssueDao">
	<select id="listJobIssue" resultType="jobissue" >
		select * from jobissue
	</select>
	<!--  <insert id="addJobIssue" parameterType="jobissue">
		insert into jobissue values(#{deptname},#{jobstyle},#{jobno},#{jobsort},#{jobname},#{recruitnum},#{enddate},#{registerdate},#{jobdescribe},#{jobrequire})
	</insert> -->
	<!--<delete id="delBook" parameterType="int">
		delete from jobissue where id=#{id}	
	</delete> -->
</mapper>